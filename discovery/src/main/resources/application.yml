server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///C:/config-repo
          clone-on-start: true
          # optional: pick a branch
          default-label: main
